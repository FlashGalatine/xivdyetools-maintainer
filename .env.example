# XIV Dye Tools Maintainer - Environment Configuration
# Copy this file to .env and fill in the values

# ============================================================================
# API Key for Maintainer Service Authentication
# ============================================================================
# This key is required for write operations (adding/modifying dyes).
# Generate a secure random key using:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# The same key must be set in both:
#   - .env file (VITE_MAINTAINER_API_KEY) - for the client
#   - Environment when running server (MAINTAINER_API_KEY) - for the server
#
# Example usage:
#   1. Generate a key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   2. Add to .env: VITE_MAINTAINER_API_KEY=your-generated-key
#   3. Run server: MAINTAINER_API_KEY=your-generated-key npm run dev:server
#   4. Or use: export MAINTAINER_API_KEY=your-generated-key && npm run dev

VITE_MAINTAINER_API_KEY=
MAINTAINER_API_KEY=
