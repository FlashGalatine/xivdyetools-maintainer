# XIV Dye Tools Maintainer - Environment Configuration
# Copy this file to .env and fill in the values

# ============================================================================
# API Key for Server-Side Authentication (Optional)
# ============================================================================
# This key provides fallback authentication for direct server API calls.
# The frontend uses session-based authentication (no API key needed).
#
# To set the API key:
#   1. Generate a key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   2. Set environment variable: export MAINTAINER_API_KEY=your-generated-key
#   3. Run server: npm run dev:server
#
# Note: The frontend no longer requires VITE_MAINTAINER_API_KEY.
# Authentication is handled via session tokens obtained automatically.

MAINTAINER_API_KEY=
